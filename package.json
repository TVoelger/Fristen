{
  "name": "fristen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fristenübersicht für die Kanzlei – Webversion mit späterer Desktop-Oberfläche",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "test:ui": "playwright test"
  },
  "dependencies": { "lucide": "1.45.0" },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
